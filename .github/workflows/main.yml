name: first_workflow

on: 
  push:
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: run a sample github action
      run : echo"this workflow run successfull for this particular action"
